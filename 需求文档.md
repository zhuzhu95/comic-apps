# 一个看漫画的APP

1. 可以导入已经下载的漫画资源，可以是图片、PDF、压缩包（包里是图片）格式。
2. 根据漫画的首页来做封面，图片格式的资源就是取第一张图片，PDF就是取第一页。
3. 支持按照名称模糊搜索
4. 支持列表样式和平铺样式切换，列表样式就只显示漫画名称；平铺就是有封面样式，不需要单独显示名称了。
5. 除了导入漫画资源这个操作，也可以直接指定文件夹位置，比如 D:\Comic\18\ 这样的文件夹，这个文件夹下面都是漫画资源，每一个漫画资源都是一个文件夹，类似这样：D:\Comic\18\FGO-Saber-01\FGO-Saber-01.zip，或者FGO-Saber-01这个文件夹下面就是01.png~22.png这样的22张图片。直接使用指定的文件夹当作资源库，这也是我希望的。直接在这个文件夹下面生成一个文件夹存放需要的封面和漫画名称等需要使用的资源就好了。当然如果需要搭建数据库来存放一些数据，那么也可以。
6. 先开发一个windows版本的app，使用Tauri框架开发，里面的javascript可以使用vue3.6

上面就是我暂时的想法，有需要确认的、或者模糊的地方可以先询问我。
